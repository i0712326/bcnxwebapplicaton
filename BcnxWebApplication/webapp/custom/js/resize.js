$(document).ready(function(){
    var userWidth = screen.width-500;
    var userHeight = screen.height-500;

    function resizeContent()
    {
        //document.getElementById("wrapper").style.width = parseInt(userWidth);
        //  document.getElementById("wrapper").style.height = parseInt(userHeight);
    }
});